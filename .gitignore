# Personal / internal files — never upload these to the public repo
CLAUDE.md
DECISIONS_AND_LEARNINGS.md
IDEAS.md
conversation_logs/
.claude/

# Personal documents (CV etc.) — never upload to the public repo
*.pdf

# Figma / FigJam source files — huge binaries, never upload to the public repo
*.jam
*.fig

# macOS clutter
.DS_Store
